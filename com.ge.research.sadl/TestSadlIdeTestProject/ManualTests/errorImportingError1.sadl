uri "http://sadl.imp/errorImportingError1".

import "ManualTests/errorDualPropertyType.sadl" as error.
import "empty.sadl" as empty. 

// Imported file has an error. Should be reported on load and build.
// Note: in SADL V2 import only uses the OWL file so if an OWL model file is created an error will
//	not necessarily be detected

MyOtherThing is a top-level class.
 